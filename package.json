{
  "name": "tsgames",
  "version": "1.0.0",
  "author": "oLsen",
  "license": "MIT",
  "dependencies": {
    "@types/jquery": "^3.5.14",
    "jquery": "^3.6.0",
    "node-sass": "^7.0.1",
    "typescript": "^4.6.4",
    "uglify-js": "^3.15.5"
  },
  "devDependencies": {
    "@types/chai": "^4.3.1",
    "@types/mocha": "^9.1.1",
    "@types/node": "^17.0.33",
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "ts-node": "^10.7.0"
  },
  "scripts": {
    "compileAll": "yarn run compileBaseSass && yarn run compileBS && yarn run compileCanvas && yarn run compileGoL && yarn run compileMW && yarn run compileSnake",
    "compileBaseSass": "node-sass --output-style compressed sass/index.sass public/style.css",
    "compileBS": "tsc -p tsconfig/bs.json && node-sass --output-style compressed sass/bs.sass public/bs/style.css",
    "compileCanvas": "tsc -p tsconfig/canvas.json && node-sass --output-style compressed sass/canvas.sass public/canvas/style.css",
    "compileGoL": "tsc -p tsconfig/gol.json && node-sass --output-style compressed sass/gol.sass public/gol/style.css",
    "compileMW": "tsc -p tsconfig/mw.json && node-sass --output-style compressed sass/mw.sass public/mw/style.css",
    "compileSnake": "tsc -p tsconfig/snake.json && node-sass --output-style compressed sass/snake.sass public/snake/style.css",
    "test": "tsc -p tsconfig/test.json && mocha -r ts-node/register $NODE_DEBUG_OPTION test/**/**.test.ts"
  }
}
